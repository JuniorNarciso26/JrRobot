# Branches

- `main`: baseline estável.
- `develop`: integração.
- `feature/*`: novas capacidades a partir de `develop`.
- `fix/*`: correções isoladas a partir de `develop`.
- `archive/*`: snapshots históricos.

A feature atual usa `feature/voice-playground-v1` e será integrada em `develop` por Pull Request.
