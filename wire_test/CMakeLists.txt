cmake_minimum_required(VERSION 3.16)
set(IDF_TARGET "esp32s3" CACHE STRING "JrBot wire-test target")
if(NOT IDF_TARGET STREQUAL "esp32s3")
    message(FATAL_ERROR "JrBot wire test requires esp32s3")
endif()
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(jrbot_wire_test)
